/**
 * The SDK's own diagnostic logging.
 *
 * <p><b>Not versioned surface.</b> {@code Debug} in {@code api.util} is what a bot writes; this is what the
 * library itself writes, and its shape is nobody's business but the library's.
 */
@Internal("Debug is the bot's logging; this is the library's own")
package com.botmaker.sdk.internal.trace;

import com.botmaker.plugin.api.meta.Internal;
